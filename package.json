{
  "name": "pathable-mailer",
  "private": true,
  "scripts": {
    "start": "meteor run  --port 3003",
    "test": "meteor test --driver-package avital:mocha --port 4003"
  },
  "dependencies": {
    "meteor-node-stubs": "~0.2.0"
  }
}
